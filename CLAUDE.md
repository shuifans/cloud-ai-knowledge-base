# CLAUDE.md — 本仓库的 AI 协作写作规范

本文件是写给 AI 协作助手（Claude 等）的写作与维护规范。人类作者同样适用。

## 站点概况

- 个人知识库网站：**云与 AI 知识体系**，作者为从业十余年的云解决方案架构师
- 技术栈：VitePress 1.x + vitepress-plugin-mermaid，纯中文
- 分类体系：四大支柱
  - **云计算** `docs/cloud/`：foundation（基座）/ infra（计算·存储·网络）/ data（数据库·大数据）/ native（云原生）
  - **人工智能** `docs/ai/`：training（模型训练）/ inference（推理与算力）/ application（大模型应用）
  - **方法论** `docs/methodology/` · **编年史** `docs/chronicle/`
- 内容分级：完整文章（无标记）· 提纲页（🚧 提示块 + 要点大纲）

## 常用命令

```bash
npm run docs:dev      # 本地预览（热更新）
npm run docs:build    # 构建（含死链检查，发布前必跑）
npm run docs:preview  # 预览构建产物
```

## 新增文章流程

1. 从 `templates/article-template.md` 复制，放入对应知识域目录，英文短横线文件名
2. 在 `docs/.vitepress/config.mts` 的对应 `sidebar` 中注册条目（提纲页标注"（提纲）"）
3. 若文章被域导读页的清单引用，同步更新导读页表格中的状态
4. 跑 `npm run docs:build` 确认无死链

## 文章结构标准

> 是什么 → 为什么 → 架构/原理（配 Mermaid 图）→ 实践与选型要点 → 常见坑 → 参考资料

- 开篇必须有引语块（`>`），说明目标读者与读完的收获
- 每篇完整文章至少 1 张 Mermaid 图，图上要有决策含义
- 决策类内容优先用表格；经验值要写明适用边界
- 文末"站内相关"互链，维护知识网络

## 写作风格

- 第一人称从业者视角，讲"一线怎么用、坑在哪"，不做论文综述
- 观点要敢下判断，但标明经验边界（"多数场景/我遇到的情况是"）
- 术语首次出现给一句话解释；产品名用通用名 + 括号标注典型产品（如"托管 K8s（ACK 类）"）

## 合规红线（发布前强制自检）

1. **不出现任何客户名称**、可识别的客户项目信息
2. **不出现内部系统名、内部工具、内部代号**
3. **不使用未公开数据**（内部报价、容量、性能数据等），只用公开资料或量级化表述
4. 案例一律以"某行业客户""典型场景"泛化
5. 涉及阿里云产品处，以官方公开文档为准，不引申未公开能力

## 提纲页扩充约定

提纲页正文以 `::: warning 🚧 本文是提纲页 ... :::` 开头。扩充为完整文章时：移除该提示块、按文章结构标准补全、更新导读页与模板中的状态标记。

## 发布流程

- 推送到 GitHub 仓库 `main` 分支，GitHub Actions 自动构建并部署到 Pages
- 提交信息用中文，格式：`新增/更新/修复: <文章或模块>`
